# 智能客服系统Demo

基于Vue 3 + FastAPI的现代化智能客服系统，支持多Agent协同、RAG检索增强生成、WebSocket实时通信、完整的用户认证和会话管理。

![系统架构](https://img.shields.io/badge/架构-微服务-blue) ![前端](https://img.shields.io/badge/前端-Vue3-green) ![后端](https://img.shields.io/badge/后端-FastAPI-orange) ![数据库](https://img.shields.io/badge/数据库-MySQL%2BRedis%2BChromaDB-red)

## ✨ 核心特性

### 🎯 智能对话
- **多Agent协同系统**: 意图路由器、订单处理、售后服务、产品咨询等专业Agent
- **RAG检索增强生成**: 基于ChromaDB向量数据库的智能知识检索
- **实时WebSocket通信**: 支持流式输出和实时对话
- **会话上下文管理**: Redis缓存支持，保持对话连续性

### 🔐 安全认证
- **JWT身份验证**: 完整的登录、注册、token管理
- **权限控制**: 基于角色的访问控制
- **Redis黑名单**: 支持token主动失效

### 🗄️ 数据管理
- **MySQL关系数据库**: 用户数据、对话记录、订单信息存储
- **Redis缓存**: 会话状态、用户认证、热点数据缓存
- **ChromaDB向量数据库**: 文档向量化存储，智能语义检索

### 📊 监控运维
- **Prometheus指标**: 系统性能监控，API调用统计
- **结构化日志**: 多级别日志记录，便于问题排查
- **健康检查**: 服务状态监控和报警

### 🚀 部署运维
- **Docker容器化**: 一键部署，支持多环境
- **Nginx反向代理**: 生产环境负载均衡
- **数据库迁移**: Alembic版本管理

## 🏗️ 系统架构

```
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   前端界面层      │    │   API网关层      │    │   业务逻辑层      │
│                │    │                │    │                │
│  Vue 3 + TS    │◄──►│  FastAPI        │◄──►│  Multi-Agent    │
│  Element Plus   │    │  WebSocket      │    │  RAG Pipeline   │
│  Pinia Store    │    │  JWT Auth       │    │  Chat Service   │
└─────────────────┘    └─────────────────┘    └─────────────────┘
                                                        │
┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐
│   数据存储层      │    │   缓存层        │    │   向量数据库层    │
│                │    │                │    │                │
│  MySQL 8.0     │    │  Redis 7       │    │  ChromaDB       │
│  用户/订单数据   │    │  会话/JWT缓存   │    │  知识向量化     │
└─────────────────┘    └─────────────────┘    └─────────────────┘
```

### 🧠 多Agent系统架构

```
                    ┌─────────────┐
                    │ 协调调度器   │
                    │Coordinator  │
                    └──────┬──────┘
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
┌───────▼───────┐  ┌──────▼───────┐  ┌──────▼───────┐
│  意图路由Agent │  │  产品咨询Agent│  │  订单处理Agent│
│Intent Router  │  │Product Agent │  │Order Agent   │
└───────┬───────┘  └──────┬───────┘  └──────┬───────┘
        │                  │                  │
        └──────────────────┼──────────────────┘
                           │
                ┌──────────▼──────────┐
                │   售后服务Agent      │
                │AfterSales Agent     │
                └─────────────────────┘
```

## 🚀 快速开始

### 环境要求

- **Python**: 3.8+
- **Node.js**: 16+ 
- **MySQL**: 8.0+
- **Redis**: 7+
- **Docker**: 20.10+ (可选)

### 方法1: Docker Compose部署 (推荐)

```bash
# 启动所有服务 (MySQL, Redis, 前端, 后端)
docker-compose up -d

# 查看服务状态
docker-compose ps
```

访问地址:
- 前端界面: http://localhost:3000
- 后端API: http://localhost:8000
- API文档: http://localhost:8000/docs
- Prometheus监控: http://localhost:9090

### 方法2: 手动部署

#### 1. 数据库准备

```bash
# 启动MySQL和Redis (使用Docker)
docker run -d --name mysql -p 3306:3306 -e MYSQL_ROOT_PASSWORD=011216 -e MYSQL_DATABASE=customer mysql:8.0
docker run -d --name redis -p 6379:6379 redis:7-alpine

# 初始化数据库表结构
cd scripts/database
python init_database.py
python migrations/initial_migration.py
```

#### 2. 后端服务

```bash
# 安装依赖
cd backend
pip install -r requirements.txt

# 设置环境变量
cp .env .env.local
# 编辑.env.local配置数据库连接信息

# 启动服务
python main.py
# 或使用
uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

#### 3. 前端服务

```bash
# 安装依赖
cd frontend
npm install

# 启动开发服务器
npm run dev
```

## 📚 API文档

### 认证接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/v1/auth/login` | 用户登录 |
| POST | `/api/v1/auth/register` | 用户注册 |
| POST | `/api/v1/auth/refresh` | 刷新token |
| POST | `/api/v1/auth/logout` | 用户登出 |

### 聊天接口

| 方法 | 路径 | 描述 |
|------|------|------|
| POST | `/api/v1/chat` | 发送聊天消息 |
| WebSocket | `/api/v1/chat/ws/{session_id}` | WebSocket实时对话 |

### 系统监控

| 方法 | 路径 | 描述 |
|------|------|------|
| GET | `/api/v1/metrics` | Prometheus指标 |
| GET | `/health` | 健康检查 |

## 🔧 配置说明

### 环境变量配置

```bash
# 数据库配置
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=011216
MYSQL_DATABASE=customer

# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

# JWT配置
SECRET_KEY=your-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# LLM配置
OPENAI_API_KEY=your-openai-api-key
DASHSCOPE_API_KEY=your-dashscope-api-key
```

### 多Agent配置

系统支持以下专业Agent:

- **IntentRouterAgent**: 用户意图识别和路由分发
- **ProductAgent**: 产品咨询和问题解答
- **OrderAgent**: 订单查询、状态跟踪
- **AfterSalesAgent**: 退换货、售后服务

## 🧪 测试

### API测试

```bash
# 启动后端后，使用curl测试
curl -X POST "http://localhost:8000/api/v1/auth/login" \
     -H "Content-Type: application/json" \
     -d '{"username":"test","password":"123456"}'

# WebSocket测试
# 连接 ws://localhost:8000/api/v1/chat/ws/test-session
```

### 端到端测试

```bash
# 前端测试
cd frontend
npm run test

# 后端测试
cd backend
pytest tests/ -v
```

## 📦 项目结构

```
server_demo/
├── backend/                    # 后端服务
│   ├── app/
│   │   ├── api/v1/            # API路由
│   │   │   ├── auth.py        # 认证接口
│   │   │   ├── chat.py        # 聊天接口
│   │   │   └── metrics.py     # 监控接口
│   │   ├── core/              # 核心配置
│   │   │   ├── config.py      # 应用配置
│   │   │   └── security.py    # 安全认证
│   │   ├── managers/          # 管理器
│   │   │   ├── mysql_manager.py   # MySQL管理
│   │   │   ├── redis_manager.py   # Redis管理
│   │   │   ├── session_manager.py # 会话管理
│   │   │   └── prometheus_manager.py # 指标管理
│   │   ├── services/          # 业务服务
│   │   │   ├── agents/        # 多Agent系统
│   │   │   ├── tools/         # 工具类
│   │   │   ├── chat_service.py    # 聊天服务
│   │   │   ├── multi_agent_system.py # 多Agent协调
│   │   │   └── rag_pipeline.py    # RAG管道
│   │   ├── middleware/        # 中间件
│   │   └── models/            # 数据模型
│   ├── alembic/               # 数据库迁移
│   ├── logs/                  # 日志文件
│   ├── requirements.txt       # Python依赖
│   └── main.py               # 应用入口
├── frontend/                  # 前端应用
│   ├── src/
│   │   ├── components/        # Vue组件
│   │   ├── stores/            # Pinia状态管理
│   │   ├── router/            # 路由配置
│   │   ├── types/             # TypeScript类型
│   │   └── utils/             # 工具函数
│   ├── package.json           # Node.js依赖
│   └── vite.config.ts         # Vite配置
├── scripts/                   # 脚本工具
│   ├── database/              # 数据库脚本
│   ├── development/           # 开发工具
│   └── startup/               # 启动脚本
├── docker-compose.yml         # Docker编排
└── README.md                  # 项目文档
```

## 🔍 核心功能详解

### 1. 多Agent协同机制

系统采用分层Agent架构:

1. **协调调度器(Coordinator)**: 接收用户消息，进行意图识别
2. **专业Agent**: 根据意图路由到对应的专业Agent
3. **工具调用**: Agent可以使用数据库、Redis、日志等工具
4. **结果整合**: 协调器整合各Agent结果，返回最终响应

### 2. RAG检索增强生成

```python
# 知识文档向量化存储
vector_store = Chroma.from_documents(
    documents=texts,
    embedding=embeddings,
    persist_directory=chroma_db_path
)

# 基于语义相似度检索
retrieved_docs = vector_store.similarity_search(
    query=user_message,
    k=3  # 返回最相关的3个文档
)
```

### 3. 会话状态管理

- **Redis缓存**: 会话上下文、用户状态
- **MySQL持久化**: 对话历史、用户信息
- **JWT认证**: 无状态的用户身份验证

## 🚀 部署指南

### 开发环境

```bash
# 克隆项目
git clone <repository-url>
cd server_demo

# 使用开发环境配置
cp .env.development .env

# 启动开发服务
./scripts/startup/start_docker.bat  # Windows
# 或
./scripts/startup/start_docker.sh   # Linux/Mac
```

### 生产环境

```bash
# 使用生产环境配置
cp .env.production .env

# 构建生产镜像
docker-compose -f docker-compose.prod.yml build

# 启动生产服务
docker-compose -f docker-compose.prod.yml up -d
```

## 🛠️ 开发指南

### 添加新的Agent

1. 在 `backend/app/services/agents/` 下创建新的Agent类
2. 实现 `process_message` 方法
3. 在 `multi_agent_system.py` 中注册新Agent
4. 更新意图路由逻辑

### 自定义知识库

1. 将文档放入 `knowledge/` 目录
2. 运行文档向量化脚本: `python scripts/development/create_sample_pdf.py`
3. 重启服务加载新的知识库

## 📊 监控指标

系统提供以下监控指标:

- **API请求量**: `api_requests_total`
- **响应时间**: `api_response_time_seconds`
- **错误率**: `api_errors_total`
- **活跃用户数**: `active_users`
- **对话轮数**: `chat_turns_total`

## 🤝 贡献指南

1. Fork 项目
2. 创建功能分支: `git checkout -b feature/new-feature`
3. 提交更改: `git commit -am 'Add new feature'`
4. 推送分支: `git push origin feature/new-feature`
5. 创建Pull Request

## 📄 许可证

本项目采用 MIT 许可证 - 查看 [LICENSE](LICENSE) 文件了解详情

## 📞 技术支持

如有问题或建议，请:

1. 查看 [API文档](http://localhost:8000/docs)
2. 提交 [Issue](https://github.com/your-repo/issues)
3. 联系维护团队

---

**智能客服系统Demo** - 让AI为您和您的客户提供更好的服务体验 🚀